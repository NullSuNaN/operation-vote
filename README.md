# Operation Voting

have real-time votes on keyboard/mouse actions.

## Note

You can make your own servers and clients with the `core/` stuff.

The `client-window`, `server` are all for default keyboard/mouse actions voting.

## Examples

```bash
server 127.0.0.1 9055 http://127.0.0.1:9056/
```